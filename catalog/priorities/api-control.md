<!-- Generated by scripts/build_catalog.py. Do not edit by hand. -->

# API and limits

28 questions carry a firm `api_control` label: 1 appear in a candidate sitting, 10 in a compiled report, 17 in a study bank. A question counted in two sources is counted in each.

What the candidate is asked to do: design 18, implement 7, explain 1.

These are counts of questions that were written down. They are not the probability that anyone asks them. Company names are tags copied from a bank unless the cell says *stated*, which means the candidate named the employer.

[← back to the catalog](../README.md) · [the report](../../REPORT.md) · [the labeled rows](../../data/unified_labeled.csv)

## Asked in a sitting — 1

A person described this interview, or an interviewer said they asked it.

| ID | Question | Task | Named in the text | Company tags | Sources |
|---|---|---|---|---|---|
| `UQ-1758` | Where do circuit breakers, rate limiting, and fault tolerance sit in an agent system? | explain | rate limiting, circuit breaker | **Sopra Steria** (stated) | Pksingh900 |

## In a compiled report — 10

A compiled list marks it reported and cites a source we did not open.

| ID | Question | Task | Named in the text | Company tags | Sources |
|---|---|---|---|---|---|
| `UQ-0388` | Build a rate limiter | _design_ (review) | rate limiting | Anthropic | landedjobs |
| `UQ-0394` | Build a Webhook Delivery System: register endpoints, receive events, deliver reliably, retries with backoff, dead letter queue for permanently failed stuff, and an API to check status | design | — | — | landedjobs |
| `UQ-0523` | Design a real-time chatbot API | design | — | — | landedjobs |
| `UQ-0551` | Design an AI gateway/proxy for managing LLM access across an organization. | design | — | — | landedjobs |
| `UQ-0571` | Design an integration between a client CRM and your platform's API. The client uses OAuth 1.0 and yours uses OAuth 2.0. Walk through your approach. | design | — | — | landedjobs |
| `UQ-0779` | Given a stream of user queries to our LLM API, design an algorithm to detect and flag potentially abusive or rate-limiting requests in real-time. You can assume queries have user IDs and timestamps. | design | rate limiting | — | landedjobs |
| `UQ-1116` | Implement a flexible rate-limiter for API endpoints using per-user rules and interfaces | implement | rate limiting | Cursor | landedjobs |
| `UQ-1127` | Implement a rate limiter for an LLM API gateway: each user gets X requests/minute and Y tokens/minute. `allow(user, tokens)` returns whether the request may proceed. Then make it work across multiple gateway instances. | implement | rate limiting | xAI | landedjobs |
| `UQ-1130` | Implement a retry mechanism with exponential backoff for LLM API calls. | implement | — | Anthropic | landedjobs |
| `UQ-1421` | Walk through how you would set up a reliable webhook integration with a client system that frequently goes offline. | design | — | — | landedjobs |

## In a study bank — 17

A coach or a bank author wrote it as a topic to study.

| ID | Question | Task | Named in the text | Company tags | Sources |
|---|---|---|---|---|---|
| `UQ-0012` | Implement a token-bucket rate limiter, then make it distributed. | implement | rate limiting | Anthropic, Cohere, OpenAI +1 more | pallavi-shekhar |
| `UQ-0061` | Build a rate limiter. Every ten minutes I'll add a requirement: per-tenant limits, burst allowances, then a sliding window. How do you keep your code from collapsing? | design | rate limiting | Anthropic | ombharatiya, pallavi-shekhar |
| `UQ-0080` | Design a rate limiter for an LLM API where cost scales with tokens, not requests. | design | rate limiting | xAI | ombharatiya, pallavi-shekhar |
| `UQ-0086` | Design a token-based rate limiter for a multi-tenant LLM API. Implement the core, then tell me what changes when it's distributed. | design | rate limiting, multi-tenant | Cohere | ombharatiya, pallavi-shekhar |
| `UQ-0096` | Design an LLM gateway: routing across providers, failover, caching, budgets and rate limits. | design | rate limiting | Palantir, Perplexity | pallavi-shekhar |
| `UQ-0184` | Implement a client pool over multiple LLM providers with failover: providers fail, time out, or rate-limit, and callers should just get a completion. | implement | rate limiting | Perplexity | ombharatiya, pallavi-shekhar |
| `UQ-0254` | Write an async batch processor over an API with concurrency limits, retries with jitter and error isolation. | implement | — | Anthropic, Perplexity | pallavi-shekhar |
| `UQ-0389` | Build a rate limiter for an API gateway: per-key token bucket first, then extend it to multiple gateway instances. | design | rate limiting | OpenAI | ombharatiya |
| `UQ-0509` | Design a multi-provider LLM gateway: routing, fallbacks, and the pitfalls teams hit. | design | — | — | ombharatiya |
| `UQ-0580` | Design and build a webhook delivery system that reliably delivers events to customer-registered URLs. | design | — | OpenAI | pallavi-shekhar |
| `UQ-0584` | Design APIs for developers to access Anthropic's models securely and efficiently. | design | — | Anthropic | pallavi-shekhar |
| `UQ-0627` | Design the reliability layer for calls to an LLM provider: timeouts, retries, circuit breakers, idempotency. | design | idempotency, circuit breaker | — | ombharatiya |
| `UQ-1673` | What would you do if a customer's CRM has rate-limited the API in the middle of a sequence? | _design_ (review) | rate limiting | — | landedjobs |
| `UQ-1756` | When would you use a batch API, and how do you design a pipeline around one? | design | — | — | ombharatiya |
| `UQ-1897` | You need to run an LLM call over 50,000 documents. The API allows ~100 concurrent requests and occasionally returns 429s and timeouts. Write the Python. | implement | — | Anthropic | ombharatiya |
| `UQ-1909` | You suspect someone is distilling your model through your public API. How would you detect it and what can you actually do? | design | — | — | ombharatiya |
| `UQ-1948` | Your app is getting 429s from your LLM provider at peak traffic. How do you handle rate limits properly? | design | rate limiting | — | ombharatiya |

## Labeled `api_control`, but not firmly — 8

The classifier returned this priority below 0.6 confidence, or returned `unclear`. These rows stay in the data and stay out of every count above.

<details>
<summary>Show 8 review rows</summary>

| ID | Question | Task | Named in the text | Company tags | Sources |
|---|---|---|---|---|---|
| `UQ-0338` | A user closes the tab halfway through a streamed response. What happens on the server, and what should happen? | explain | — | — | ombharatiya |
| `UQ-0581` | Design and build a webhook delivery system: your service must deliver events to customer-registered URLs reliably. | design | — | OpenAI | ombharatiya |
| `UQ-0896` | How do you handle a model deprecation that breaks a customer? | _design_ (review) | — | — | landedjobs |
| `UQ-1168` | Implement streaming responses for an LLM API. | implement | — | OpenAI | landedjobs |
| `UQ-1518` | What is a denial-of-wallet attack, and how do you defend against it? | explain | — | — | ombharatiya |
| `UQ-1787` | Why do LLM products stream responses, and how does streaming actually work over HTTP? | explain | — | — | ombharatiya |
| `UQ-1855` | Write a service that proxies streaming TTS to a browser and cancels cleanly when the user navigates away. | implement | — | ElevenLabs | pallavi-shekhar |
| `UQ-1915` | You want to switch model providers and your prompts break. Why, and how would you have made them portable? | _diagnose_ (review) | — | — | ombharatiya |

</details>
