<!-- Generated by scripts/build_catalog.py. Do not edit by hand. -->

# Coding

100 questions carry a firm `coding` label: 1 appear in a candidate sitting, 52 in a compiled report, 47 in a study bank. A question counted in two sources is counted in each.

What the candidate is asked to do: implement 76, design 7, explain 4, diagnose 4.

These are counts of questions that were written down. They are not the probability that anyone asks them. Company names are tags copied from a bank unless the cell says *stated*, which means the candidate named the employer.

[← back to the catalog](../README.md) · [the report](../../REPORT.md) · [the labeled rows](../../data/unified_labeled.csv)

## Asked in a sitting — 1

A person described this interview, or an interviewer said they asked it.

| ID | Question | Task | Named in the text | Company tags | Sources |
|---|---|---|---|---|---|
| `UQ-0418` | Coding question in Python (prompt not stated in the post). | _unclear_ (review) | — | — | kris_aieng |

## In a compiled report — 52

A compiled list marks it reported and cites a source we did not open.

<details>
<summary>Show all 52 questions</summary>

| ID | Question | Task | Named in the text | Company tags | Sources |
|---|---|---|---|---|---|
| `UQ-0290` | A binary string S encodes a value V; reduce V to 0 by subtracting 1 if odd or dividing by 2 if even | implement | — | — | landedjobs |
| `UQ-0316` | A merge-interval style coding question, plus an ML fundamentals probe on Precision/Recall vs ROC-AUC and what perplexity means. | explain | perplexity | — | landedjobs |
| `UQ-0333` | A substantial take-home project, about five hours of work, building something with OpenAI's APIs." Deliverables: "code and a video walkthrough explaining your solution. | implement | — | — | landedjobs |
| `UQ-0391` | Build a thread-safe cache with configurable eviction policy | _implement_ (review) | — | — | landedjobs |
| `UQ-0405` | Calibration review of unfamiliar code / PR-style exercise | _evaluate_ (review) | — | Google DeepMind | landedjobs |
| `UQ-0413` | Code conv2d from scratch. | implement | — | Microsoft | landedjobs |
| `UQ-0414` | Code review / style fix using AI assistant tools | implement | — | Meta | landedjobs |
| `UQ-0416` | Coding & Algorithms: A 60-minute live coding session" within a 6-round loop — ML-flavored algorithmic coding plus practical AI utilities (tokenizer, streaming HTTP client, eval harness glue). | implement | — | Mistral | landedjobs |
| `UQ-0456` | Day 1-2: Pick your language and write five infrastructure utilities from scratch. Rate limiter, LRU cache with TTL, batch aggregator, retry-with-backoff… | implement | rate limiting | Anthropic | landedjobs |
| `UQ-0530` | Design a SQL query to find the cumulative weekly retention of users. | implement | — | — | landedjobs |
| `UQ-0568` | Design an in-memory database with basic SQL (CREATE TABLE, INSERT, SELECT with WHERE, JOINs) | design | — | — | landedjobs |
| `UQ-0619` | Design the longest substring without repeating characters | implement | — | — | landedjobs |
| `UQ-0756` | Find dates where a stadium had three or more consecutive days of attendance >=100. | implement | — | — | landedjobs |
| `UQ-0757` | Find the intersection of two large, sorted lists of user IDs efficiently, returning a new sorted list. | implement | — | — | landedjobs |
| `UQ-0758` | Find the top 3 unique salaries in each department and list all employees who have those salaries. | implement | — | — | landedjobs |
| `UQ-0761` | Fix a bug in a given transformer/attention implementation | implement | attention | — | landedjobs |
| `UQ-0774` | Given a directed graph… return the longest dependency path" *(sample format)* | implement | — | — | landedjobs |
| `UQ-0780` | Given a toy codebase plus unit tests, you are allowed to use an LLM to debug and complete the implementation. | implement | — | — | landedjobs |
| `UQ-1119` | Implement a function that takes a list of API endpoint response times (in milliseconds) and returns the p95 latency. Handle potential errors like empty lists or non-numeric values. | implement | — | — | landedjobs |
| `UQ-1120` | Implement a function that takes a stream of strings and removes duplicates in real time, without storing the entire stream in memory | implement | — | — | landedjobs |
| `UQ-1122` | Implement a LoRA adapter from scratch | implement | LoRA | — | landedjobs |
| `UQ-1123` | Implement a metric in pandas: cohort funnel from impression to click to purchase. | implement | — | — | landedjobs |
| `UQ-1126` | Implement a prompt template system with variable substitution. | implement | — | — | landedjobs |
| `UQ-1134` | Implement a simple retriever. | implement | — | — | landedjobs |
| `UQ-1140` | Implement a training loop that supports mixed-precision, gradient accumulation, gradient clipping, cosine LR schedule with warmup, and deterministic reproducibility." A common live variant: "This training loop OOMs at b… | implement | cosine similarity | OpenAI | landedjobs |
| `UQ-1142` | Implement a transformer from scratch" *(during the LLM knowledge quiz)* | implement | — | — | landedjobs |
| `UQ-1143` | Implement a trie | implement | — | Anthropic | landedjobs |
| `UQ-1145` | Implement an async task queue: tasks can be submitted at any time, at most N run concurrently, each submission returns a promise/future of its result. (JS/TS or Python asyncio.) | implement | — | — | landedjobs |
| `UQ-1146` | Implement an LRU cache | implement | — | OpenAI | landedjobs |
| `UQ-1147` | Implement an LRU Cache (Classic but Dangerous) — implement `get(key)` and `put(key, value)` in O(1) | implement | — | — | landedjobs |
| `UQ-1149` | Implement attention [block]" / "implement PPO" / "implement VAE | implement | attention | OpenAI | landedjobs |
| `UQ-1151` | Implement Backprop for a Tiny Network" (rated hard, 137 candidate reports) and the sibling prompt "Debug MiniGPT and Backpropagate Matmul" (rated medium, 58 candidate reports). | implement | — | OpenAI | landedjobs |
| `UQ-1152` | Implement beam search decoding for an autoregressive language model: given `model(prefix) -> log_probs`, return the highest-scoring sequence with beam width k. | implement | — | Mistral | landedjobs |
| `UQ-1156` | Implement cosine similarity, dot product, and Euclidean distance functions from scratch" and "Build a simple vector similarity search from scratch. | implement | cosine similarity | — | landedjobs |
| `UQ-1175` | In-Memory File System Coding Challenge | implement | — | Perplexity | landedjobs |
| `UQ-1200` | Low-level CUDA kernel implementation | implement | — | NVIDIA | landedjobs |
| `UQ-1207` | Merge sorted lists | implement | — | Anthropic | landedjobs |
| `UQ-1233` | Parse data and compute statistics transformations (coding round); ML concepts, LLMs, and debugging (ML coding round). | implement | — | Scale | landedjobs |
| `UQ-1236` | Perplexity Interview Experience: In-Memory File System Coding Challenge. Verified candidate reports — coding. | implement | perplexity | Anthropic | landedjobs |
| `UQ-1271` | Reading unfamiliar code, spotting conceptual bugs, tradeoffs under time | diagnose | — | Scale | landedjobs |
| `UQ-1281` | Rotate a matrix | implement | — | Anthropic | landedjobs |
| `UQ-1348` | The "classic LRU Cache implementation question", reported at OpenAI's platform SWE onsite. | implement | — | OpenAI | landedjobs |
| `UQ-1357` | The interview itself is "time-limit, about 50 minutes or so, during a 60 min interview round"; the prompt is for the candidate to "rapidly build a working prototype. | implement | — | — | landedjobs |
| `UQ-1362` | The weirdest part was the final round: they dropped me into a Google Colab notebook with maybe 20 to 30 lines of skeleton Python and asked me to debug an actual LLM inference step" — and was "explicitly told not to use… | _implement_ (review) | — | Anthropic | landedjobs |
| `UQ-1374` | Trust & safety domain funnel in SQL with retention, plus a Python question on pandas dataframe transformation for CTR. | implement | — | — | landedjobs |
| `UQ-1382` | Validate a nested JSON configuration against a predefined schema, handling nested structures and various data types. | implement | — | — | landedjobs |
| `UQ-1848` | Word Search on Grid (Trie + DFS) — given an N x N character board and a dictionary, find all valid words that can be formed by adjacent letters | implement | — | — | landedjobs |
| `UQ-1850` | Write a function that returns total push notifications before user conversion from a user-event log. | implement | — | — | landedjobs |
| `UQ-1852` | Write a function to efficiently search for a specific string pattern within a large corpus of text documents. Assume documents are stored as a list of strings. | implement | — | — | landedjobs |
| `UQ-1862` | Write the third transaction of every user. | implement | — | — | landedjobs |
| `UQ-1870` | You are given a directed graph of dependencies between ML experiments; given the graph, return the longest path from any starting node" — plus standard medium LeetCode DS&A *(set reported verbatim in IGotAnOffer's compa… | implement | — | — | landedjobs |
| `UQ-1872` | You are given a transformer-based ML model with four failing unit tests — two bugs are known, two are novel. Identify, debug, and fix each bug so the model trains and evaluates correctly. Given a labeled dataset, write… | implement | — | OpenAI | landedjobs |

</details>

## In a study bank — 47

A coach or a bank author wrote it as a topic to study.

<details>
<summary>Show all 47 questions</summary>

| ID | Question | Task | Named in the text | Company tags | Sources |
|---|---|---|---|---|---|
| `UQ-0023` | Implement an LRU cache with O(1) get/put, then add TTL. | implement | — | Alibaba, OpenAI, xAI | pallavi-shekhar |
| `UQ-0062` | Build an in-memory key-value store with SET/GET/DELETE, then add transactions with BEGIN/COMMIT/ROLLBACK - including nested transactions. | implement | — | xAI | ombharatiya, pallavi-shekhar |
| `UQ-0185` | Implement a thread-safe batching logger: many producer threads call `log(msg)`, and a background thread flushes batches of up to 100 messages every second or when full, whichever comes first. | implement | — | Databricks | ombharatiya, pallavi-shekhar |
| `UQ-0186` | Implement a top-k MoE router in PyTorch. Then contrast auxiliary-loss load balancing with a loss-free approach. | implement | MoE, PyTorch | Zhipu AI, Zhipu AI (GLM) | ombharatiya, pallavi-shekhar |
| `UQ-0188` | Implement top-k MoE routing with a shared expert in PyTorch, and point out where the efficiency and correctness traps are. | implement | MoE, PyTorch | DeepSeek | ombharatiya, pallavi-shekhar |
| `UQ-0255` | Write an iterator class that lazily flattens an arbitrarily nested list of lists/integers. No generators - explicit state. | implement | — | xAI | ombharatiya, pallavi-shekhar |
| `UQ-0259` | Write the server-side handler for streaming token generation. Handle client disconnects correctly. | implement | — | Together AI | ombharatiya, pallavi-shekhar |
| `UQ-0271` | You're given exports from three customer systems, each with its own customer records. Write code to produce one deduplicated set of entities, and explain your design. | _implement_ (review) | — | Palantir | ombharatiya, pallavi-shekhar |
| `UQ-0331` | A standard Alibaba coding round. Design a data structure for a fixed-capacity cache that supports get and put in O(1) and evicts the least-recently-used entry when full. Implement it in Python. | design | — | Alibaba | ombharatiya |
| `UQ-0384` | Build a function that fetches paginated data from a REST API, handling page size and total-page logic. | implement | — | Palantir | pallavi-shekhar |
| `UQ-0398` | Build an OOP system for managing courses, grades and students. | design | — | Anthropic | pallavi-shekhar |
| `UQ-0419` | Coding: given a repository snapshot (path → content), build a Merkle tree and write the function that returns which files changed between two snapshots without comparing every file's content. | implement | — | Cursor | ombharatiya |
| `UQ-0420` | Coding: given annotation sessions as (start, end) timestamps, return the peak number of concurrent annotators, and the intervals during which the platform was at peak load. | implement | — | Scale AI | ombharatiya |
| `UQ-0583` | Design and implement an in-memory key-value store supporting set, transactional begin, commit and abort. | design | — | OpenAI | pallavi-shekhar |
| `UQ-0775` | Given a helper method that crawls a URL, write a crawler over a domain: first synchronous, then make it async. | implement | — | Anthropic | pallavi-shekhar |
| `UQ-0776` | Given a list of allowed IP ranges as CIDR blocks plus explicit deny ranges, implement `is_allowed(ip)` efficiently for millions of checks per second. | implement | — | Databricks | ombharatiya |
| `UQ-0778` | Given a repository snapshot (path → content), build a Merkle tree and write the function returning which files changed between two snapshots without comparing every file's content. | implement | — | Cursor (Anysphere) | pallavi-shekhar |
| `UQ-0781` | Given allowed IP ranges as CIDR blocks plus explicit deny ranges, implement is_allowed(ip) efficiently for millions of checks per second. | implement | — | Databricks | pallavi-shekhar |
| `UQ-0782` | Given an array nums of n integers where n > 1, return an output array (product of array except self). | implement | — | Meta (Superintelligence Labs, FAIR, Llama) | pallavi-shekhar |
| `UQ-0783` | Given annotation sessions as (start, end) timestamps, return the peak number of concurrent annotators and the intervals at peak load. | implement | — | Scale AI | pallavi-shekhar |
| `UQ-0792` | Here are ~120 lines of working but messy code with passing tests. Improve the architecture without breaking them. What do you change first? | _implement_ (review) | — | OpenAI | ombharatiya |
| `UQ-0794` | Here's a CUDA kernel that's 10x slower than expected. Without running it, what are the usual suspects, and how do you confirm each? | diagnose | — | NVIDIA | pallavi-shekhar |
| `UQ-0795` | Here's a CUDA kernel that's 10× slower than expected. Without running it, what are the usual suspects, and how do you confirm each? | diagnose | — | NVIDIA | ombharatiya |
| `UQ-0904` | How do you handle missing data in Pandas? | explain | — | OpenAI | pallavi-shekhar |
| `UQ-1034` | How would you add `GROUP BY`? | implement | — | — | landedjobs |
| `UQ-1132` | Implement a set of shape classes that compute area, then extend them to handle a new shape. | implement | — | Palantir | pallavi-shekhar |
| `UQ-1135` | Implement a streaming top-k with a bounded-memory sketch; implement a sliding-window rate counter. | implement | — | Consumer-Scale ML Companies (Uber, Netflix, LinkedIn, Airbnb, Pintere… | pallavi-shekhar |
| `UQ-1144` | Implement a UI from a mockup with provided CSS and API. | implement | — | OpenAI | pallavi-shekhar |
| `UQ-1148` | Implement an LRU cache with O(1) get/put. Now add per-entry TTL. | implement | — | xAI | ombharatiya |
| `UQ-1161` | Implement non-maximum suppression. Then vectorise it. | implement | — | Tesla | pallavi-shekhar |
| `UQ-1170` | Implement the core of a text buffer for an editor: efficient insert/delete at arbitrary positions and fast line lookup. What structure do you pick? | design | — | Cursor | ombharatiya |
| `UQ-1171` | Implement the core of an editor text buffer: efficient insert/delete at arbitrary positions and fast line lookup. What structure do you pick? | design | — | Cursor (Anysphere) | pallavi-shekhar |
| `UQ-1241` | Practical coding: write a service that proxies streaming TTS to a browser and cancels cleanly when the user navigates away. | implement | — | ElevenLabs | ombharatiya |
| `UQ-1262` | Qwen2.5-Coder trains with repository-level fill-in-the-middle using special tokens like `<\|fim_prefix\|>`, `<\|fim_suffix\|>`, `<\|fim_middle\|>`, `<\|repo_name\|>`, and `<\|file_sep\|>`. Write the function that format… | implement | — | Alibaba | ombharatiya |
| `UQ-1276` | Refactor bad code: here are ~120 lines of working but messy code with passing tests. Improve the architecture without breaking them. What do you change first? | implement | — | OpenAI | pallavi-shekhar |
| `UQ-1284` | Second-order follow-up on user-level history aggregation; using CTEs vs nested subqueries.? | explain | — | — | landedjobs |
| `UQ-1377` | Two-part coding warm-up: given a stream of user actions, return the k most engaged-with items. Then: why might your heap solution be the wrong choice in production? | _implement_ (review) | — | Meta | ombharatiya |
| `UQ-1383` | Vectorize; edge cases; why im2col works.? | explain | — | Microsoft | landedjobs |
| `UQ-1851` | Write a function to compute root-mean-square error given y_pred and y_true lists. | implement | — | Google DeepMind and Google AI | pallavi-shekhar |
| `UQ-1853` | Write a parser that consumes a streamed LLM response (server-sent events) arriving in arbitrary network chunks and yields complete JSON events. | implement | chunking | Cohere | ombharatiya |
| `UQ-1854` | Write a Python function that displays the first n Fibonacci numbers. | implement | — | OpenAI | pallavi-shekhar |
| `UQ-1856` | Write a SQL query that joins and aggregates across tables to answer a business question. | implement | — | Palantir | pallavi-shekhar |
| `UQ-1857` | Write a streaming SSE/JSON parser that handles arbitrary chunk boundaries. | implement | chunking | Cohere | pallavi-shekhar |
| `UQ-1858` | Write an efficient ring buffer for high-rate sensor data with a fixed memory budget. | _implement_ (review) | — | Tesla | pallavi-shekhar |
| `UQ-1891` | You inherit an 800-line pipeline script from a previous deployment. It's slow and occasionally produces wrong numbers. The original author is gone. Go. | diagnose | — | Palantir | ombharatiya |
| `UQ-1923` | You're dropped into an unfamiliar multi-file codebase with a failing behaviour and an LLM assistant available. Walk me through how you'd fix it. | _implement_ (review) | — | Meta | ombharatiya |
| `UQ-1926` | You're receiving an unbounded stream of event IDs. Return the k most frequent IDs seen so far, at any point, with bounded memory. | design | — | Google DeepMind | ombharatiya |

</details>

## Labeled `coding`, but not firmly — 16

The classifier returned this priority below 0.6 confidence, or returned `unclear`. These rows stay in the data and stay out of every count above.

<details>
<summary>Show 16 review rows</summary>

| ID | Question | Task | Named in the text | Company tags | Sources |
|---|---|---|---|---|---|
| `UQ-0352` | Alternative with RANK vs DENSE_RANK; large-department scale plan.? | explain | — | — | landedjobs |
| `UQ-0396` | Build an in-memory database: SET/GET/DELETE first, then filtered scans, then TTL with timestamps, then file compaction. | implement | — | Anthropic | pallavi-shekhar |
| `UQ-0399` | Build core business logic for a toy banking application: a spec that grows in four progressive levels against a black-box evaluator. | design | — | Anthropic | pallavi-shekhar |
| `UQ-0417` | Coding exercises reported by multiple employers (cross-reference) | _implement_ (review) | — | — | landedjobs |
| `UQ-0422` | Coding: merge ranked results from N connector shards into a global top-k, applying a per-user permission filter. Do it efficiently. | implement | ACL | Glean | ombharatiya |
| `UQ-0447` | Compute entropy and implement 1-NN" (rated medium, 62 candidate reports) and "Implement 1NN with NumPy" (rated medium, 204 candidate reports). | implement | NumPy | OpenAI | landedjobs |
| `UQ-0451` | Create a dataset for sentence completion using BERT" + math/transformer probes | design | — | — | landedjobs |
| `UQ-0773` | Given a directed graph of ML experiments, deliver a working solution with AI assistance in 60 minutes | implement | — | — | landedjobs |
| `UQ-0791` | Handed a messy dataset, build a working analysis | implement | — | Scale | landedjobs |
| `UQ-1117` | Implement a full Transformer (decoder) block from memory | implement | — | — | landedjobs |
| `UQ-1118` | Implement a full transformer block in PyTorch: multi-head attention, feed-forward network, residual connections, and layer norm. You may use `nn.Linear` but nothing higher-level. | implement | attention, PyTorch | — | landedjobs |
| `UQ-1158` | Implement logistic regression from scratch (Python). | implement | — | Amazon | landedjobs |
| `UQ-1159` | Implement Multi-Head Attention from memory | implement | attention | — | landedjobs |
| `UQ-1238` | Pivoting on multiple breakdowns; memory-effective groupby; using Modin/Polars for scale.? | _explain_ (review) | — | — | landedjobs |
| `UQ-1366` | This repo contains a version of Anthropic's original performance take-home, before Claude Opus 4.5 started doing better than humans given only 2 hours." The README adds: "If you optimize below 1487 cycles, beating Claud… | implement | Claude | Anthropic | landedjobs |
| `UQ-1861` | Write code to implement token counting and context window management. | implement | context window | OpenAI | landedjobs |

</details>
